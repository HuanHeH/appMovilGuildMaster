# Committed template = production / remote API (used by deploy & fresh clones).
# For local API: copy this file to .env.local and use the LOCAL line below instead.
# Do not commit .env.local.

EXPO_PUBLIC_API_BASE_URL=https://guildmasterapi.duckdns.org/api

# --- LOCAL (put only in .env.local, not here) ---
# EXPO_PUBLIC_API_BASE_URL=http://localhost:8081/api
